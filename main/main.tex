\documentclass[11pt, a4paper]{article}

% Change your details in utility.tex
\input{../utilities/utility.tex}

% Set these three per assignment. Leave \currentpart empty if there is no part.
\newcommand{\currentsubject}{example}
\newcommand{\currentassignment}{1}
\newcommand{\currentpart}{Part 1}
\checksubject{\currentsubject}

% Packages
\usepackage{fancyhdr}
\usepackage{microtype}

% Change to a sans-serif font for better on-display reading. Helvetica ships
% with macOS only, so fall back rather than failing to build elsewhere.
\usepackage{fontspec}
\IfFontExistsTF{Helvetica}
    {\setmainfont{Helvetica}}
    {\IfFontExistsTF{TeX Gyre Heros}{\setmainfont{TeX Gyre Heros}}{}}

% Bibliography Support
\usepackage[style=apa, backend=biber]{biblatex}
\DeclareLanguageMapping{english}{english-apa}
\addbibresource{references.bib}

% Uncomment below if you want all content regardless 
% if cited or not to print in the bibliography.
% \nocite{*}   

% Loaded last so it can hook the packages above.
\usepackage{hyperref}
\hypersetup{
    hidelinks,
    pdftitle={\subjectinfo{\currentsubject} -- \assignmenttitle},
    pdfauthor={\studentname},
    pdfsubject={\subjectname{\currentsubject}},
}

\begin{document}

    % No page anchors on the title page: \pagenumbering below restarts the
    % counter at 1, which would give two PDF destinations named page.1.
    \hypersetup{pageanchor=false}

    \begin{titlepage}
    \centering
    \vspace*{\fill}

    % University Name
    \textsc{\LARGE \universityname}\\[1cm]
    
    % Subject Info
    \Large \subjectcode{\currentsubject}\\[0.5cm]
    \large \subjectname{\currentsubject}\\[0.75cm]
    
    % Assignment Title
    {\huge\bfseries \assignmenttitle}\\[2cm]
    \vspace*{1.5cm}

    % Student Info
    \begin{minipage}{0.4\textwidth}
        \begin{flushleft}
            Author\\
            \studentname
        \end{flushleft}
    \end{minipage}
    \hspace{0.1\textwidth}
    \begin{minipage}{0.4\textwidth}
        \begin{flushright}
            Student ID\\
            \studentnumber
        \end{flushright}
    \end{minipage}

    \vspace*{\fill}

    % Statement of Originality
    \statementoforiginality\\[1cm]

    \today
    
    \end{titlepage}

    \newpage
    \hypersetup{pageanchor=true}
    \pagenumbering{arabic}

    % Header Setup
    \setlength{\headheight}{15pt} 
    \pagestyle{fancy}
    \fancyhf{}
    \fancyhead[L]{\subjectinfo{\currentsubject}}
    \fancyhead[R]{\studentname\ -- \studentnumber}
    \fancyfoot[C]{\thepage}

    % Begin Content

    Start writing content here.

    % Bibliography (Only prints if cited)

    \newpage
    \printbibliography

\end{document}
