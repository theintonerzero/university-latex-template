# How to use
## Requirements
- LuaLaTeX
- Biber
## Compiling
To compile and get a `.pdf` run:
```
python3 compile.py
```
The output PDF is placed in the project root, named as `SUBJECTCODE_ASSIGNMENT_STUDENTNUMBER.pdf`.
